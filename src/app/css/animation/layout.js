export const metadata = {
  title: "Animation - Soon UI Design",
  description:
    "애니메이션 소개 페이지입니다. className을 통해 애니메이션을 추가할 수 있습니다."
};

export default function AnimationLayout({ children }) {
  return children;
}
